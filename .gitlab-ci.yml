# Use the stages from the included templates or add all necessary stages
include:
  - template: Security/SAST.gitlab-ci.yml

# Optionally define stages if needed (GitLab merges stages from all includes)
# stages: [test, analyze, ...]

# Remove the empty 'sast' job definition unless you need customizations